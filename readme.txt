Index page /index.html
Flexbox- Card components uses flexbox
Variables , Nesting- Index page uses both

home page/index.html
the main scss file is the booking.scss
Nesting- used throughout the scss file.
Variables- are being placed in config file that stores background colours used.
Placeholder selectors and extend feature- button file uses these 2 features 
Function- is used in config file which is used to change the color
Mixin- used in congif to set the background and text color of the buttons used 
